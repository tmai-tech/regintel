# Discover full PDF list for a ministry URL, then download with status tracking.
# Usage: workflow_dispatch with url=https://sdaia.gov.sa
name: Crawl ministry (list then download)

on:
  workflow_dispatch:
    inputs:
      url:
        description: "Ministry site root URL"
        required: true
        default: "https://sdaia.gov.sa"
      label:
        description: "Jurisdiction label (e.g. Saudi Arabia - SDAIA)"
        required: false
        default: "Saudi Arabia - SDAIA"
      max_pages:
        description: "Max HTML pages to scan during discovery (use 2000+ for full SDAIA)"
        required: false
        default: "2000"
      delay:
        description: "Seconds between requests"
        required: false
        default: "0.3"
      discover_only:
        description: "true = list only, do not download"
        required: false
        default: "false"

permissions:
  contents: write
  # Pages deploy is NOT done in this long job — holding environment:github-pages
  # for hours blocked every live site update. Data is git-pushed; deploy-pages.yml
  # publishes the site.

concurrency:
  group: crawl-ministry
  cancel-in-progress: true

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pymupdf || true
          # Colleague Extraction_Script path: JS-render KnowledgeCenter / SPA pages
          python -m playwright install --with-deps chromium || python -m playwright install chromium || true

      - name: Discover list + download
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REGINTEL_LIVE_GIT_PUSH: "1"
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          URL="${{ github.event.inputs.url }}"
          LABEL="${{ github.event.inputs.label }}"
          MAX_PAGES="${{ github.event.inputs.max_pages }}"
          DELAY="${{ github.event.inputs.delay }}"
          ONLY="${{ github.event.inputs.discover_only }}"
          EXTRA=()
          if [ "$ONLY" = "true" ]; then
            EXTRA+=(--discover-only)
          fi
          echo "=== Ministry pipeline: $URL ($LABEL) ==="
          python -u collector/ministry_pipeline.py \
            --url "$URL" \
            --label "$LABEL" \
            --max-pages "${MAX_PAGES:-2000}" \
            --delay "${DELAY:-0.3}" \
            "${EXTRA[@]+"${EXTRA[@]}"}"
          python scripts/fix_ministry_jurisdiction_labels.py || true
          # Public site: SDAIA + TGA + MC + MEWA (no global gazette)
          python scripts/publish_saudi_ministries.py || python scripts/publish_sdaia_only.py || true
          # print counts for summary
          python - <<'PY'
          import json
          from pathlib import Path
          p = Path("web/data/ministry_document_list.json")
          if p.exists():
              d = json.loads(p.read_text())
              c = d.get("counts") or {}
              print("COUNTS", c)
              print("phase", d.get("phase"), "label", d.get("label"))
              failed = [x for x in (d.get("documents") or []) if x.get("status") == "download_failed"]
              print("failed", len(failed))
              for f in failed[:15]:
                  print(" FAIL", f.get("filename"), f.get("download_error"))
          PY

      - name: Commit + push catalog
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          python collector/live_publish.py || true
          # After publish_sdaia_only — commit only SDAIA public payloads
          git add -f data/pdfs/manifest.json data/pdfs/ministry_lists \
            web/data/pdfs_catalog.json web/data/crawl_status.json \
            web/data/ministry_document_list.json \
            web/data/saudi_ministries.json web/data/laws_catalog.json \
            web/data/meta.json web/data/eva_summaries.json web/data/eva_meta.json \
            android/app/src/main/assets/pdfs_catalog.json \
            data/pdfs/crawl_status.json data/saudi_ministries.json 2>/dev/null || true
          REPO="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          if ! git diff --staged --quiet; then
            git commit -m "chore: ministry pipeline ${{ github.event.inputs.label }} $(date -u +%Y-%m-%dT%H:%M:%SZ)" || true
            for i in 1 2 3 4 5; do
              git fetch "$REPO" main:refs/remotes/origin/main || true
              git rebase origin/main || true
              git push "$REPO" HEAD:main && break
              sleep $((i * 3))
            done
          fi
          echo "Site updates via deploy-pages.yml on push (this job no longer holds github-pages)."
